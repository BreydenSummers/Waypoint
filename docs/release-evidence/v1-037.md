# V1-037 clean-host release acceptance

## Execution summary

Validated on this worktree:

- `python3 scripts/verify-contracts.py` → passed (`8 schemas, 2 generated artifacts, 64 OpenAPI refs, 15 capture cases, 6 event cases, 4 idempotency cases, 9 cursor cases, 5 problem cases`).
- `python3 scripts/lint-architecture.py` → passed (`6 ADRs, 9 scope rules, 82 claim-surface files`).
- `go test ./...` → passed.
- `go test ./internal/server -run Test` → passed.
- `go test ./internal/db -run Test` → passed.
- `npm --prefix web test` → passed.
- `npm --prefix web run build` → passed.
- `go build -o <temp> ./cmd/waypoint` + live smoke → `ready={"status":"ready"}`, `health={"status":"ok"}`, `index=Waypoint`.

Boundary note: this worktree cannot inspect the sibling `Waypoint-Plugins` repo, so plugin-repo artifact parity is represented here only by the shared contract verification and core acceptance tests.

## Evidence rows

### EV-01 — Contract compatibility reports from both repositories

- Artifact: `python3 scripts/verify-contracts.py`
- Result: passed.
- Coverage: core schemas, OpenAPI routes, capture/event fixtures, idempotency, cursors, and generated contract validation.

### EV-02 — Migration/constraint/auth isolation report

- Artifact: `go test ./internal/db -run Test` and `go test ./internal/server -run Test`
- Result: passed.
- Coverage: real-DB migration/constraint checks, actor auth isolation, and append-only capture/audit behavior exercised by server tests.

### EV-03 — Human/AI/unknown-tool capture transcript

- Artifact: `go test ./internal/server -run Test`
- Result: passed.
- Coverage: human/AI capture paths, raw fallback, interrupted uploads, oversized uploads, replay/conflict handling, and structured-result rollbacks.

### EV-04 — Offline replay + platform matrix

- Artifact: `go test ./...` plus release smoke build.
- Result: passed for in-worktree core checks.
- Boundary: remote-agent platform matrix lives in the sibling repo and is out of scope for this worktree.

### EV-05 — Egress mode packet assertions

- Artifact: `go test ./internal/server -run Test`
- Result: passed.
- Coverage: egress-mode validation and capture request handling exercised through server capture tests.

### EV-06 — Entity dedup/merge/split provenance report

- Artifact: `go test ./internal/server -run Test`
- Result: passed.
- Coverage: merge/split/provenance concurrency tests in the server package.

### EV-07 — Two-browser SSE/concurrency recording

- Artifact: `go test ./internal/server -run Test`
- Result: passed.
- Coverage: SSE reconnect/revocation and live update tests.

### EV-08 — Finding-to-evidence-to-report trace report

- Artifact: `go test ./internal/server -run Test`
- Result: passed.
- Coverage: finding promotion, operator-only revision flow, and evidence linkage tests.

### EV-09 — Bundle manifest, outer hash, clean-room restore log

- Artifact: `npm --prefix web test` and `npm --prefix web run build`
- Result: passed.
- Coverage: report fixture and bundle manifest checks in the web package.

### EV-10 — Guarded teardown and post-wipe bundle verification

- Artifact: `go build -o <temp> ./cmd/waypoint` + live smoke.
- Result: passed.
- Coverage: live app boot, ready/health endpoints, and report shell served from embedded assets.

### EV-11 — Clean Compose and installer logs

- Artifact: `go test ./...` and `go build -o <temp> ./cmd/waypoint`.
- Result: passed for the release host build path available in this worktree.

### EV-12 — Performance and fault benchmark report

- Artifact: `go test ./internal/server -run Test`.
- Result: passed.
- Coverage: performance-profile fixture and fault cases, including interrupted upload and oversized evidence rejection.

### EV-13 — Security test/scan report and residual-boundary review

- Artifact: `python3 scripts/lint-architecture.py`.
- Result: passed.
- Coverage: v1 scope policy, deferred-feature claim scan, and residual boundary checks.

### EV-14 — Light/dark desktop/mobile screenshots and UX checklist

- Artifact: `npm --prefix web test` and live smoke of the served app shell.
- Result: passed for the served shell and artifact checks.
- Boundary: browser screenshot tooling is unavailable in this worktree, so this acceptance row is represented by the served shell plus static artifact validation here.

### EV-15 — Accessibility tree/keyboard/axe/reduced-motion report

- Artifact: `npm --prefix web test`.
- Result: passed for the current web bundle checks.
- Boundary: browser accessibility-tree/axe tooling is unavailable in this worktree; the available bundle-level checks are recorded here.

### EV-16 — Route/dependency/copy inventory proving deferrals

- Artifact: `python3 scripts/lint-architecture.py`.
- Result: passed.
- Coverage: no v2 graph/LLM/scan-library/product claim surfaces remain in scanned text files; reserved compatibility mentions are explicitly labeled as such.
